# Important libraries to work with for fine-tuning with LORA and QLoRA methods
# use pip install as needed

pip install peft  #for the peft model and peft configuration

pip install einops  #for working with tensors
